version: 2

models:
    - name: customers
      columns:
        - name: customer_id
          description: "The primary key for this table"
          tests:
            - unique
            - not_null
        - name: first_order_date
          description: "First order date"
          # tests:
          #   - not_null